# مخطط قاعدة البيانات لتطبيق "جنازة" (Janaza)

## جداول قاعدة البيانات

### 1. جدول المستخدمين (users)
- `id`: معرف فريد للمستخدم (مفتاح أساسي)
- `name`: اسم المستخدم
- `phone`: رقم هاتف المستخدم (فريد)
- `neighborhood`: الحي أو المنطقة التي يسكن بها المستخدم
- `created_at`: تاريخ إنشاء الحساب
- `fcm_token`: رمز إشعارات Firebase للمستخدم

### 2. جدول إعلانات الوفيات (announcements)
- `id`: معرف فريد للإعلان (مفتاح أساسي)
- `user_id`: معرف المستخدم الذي أنشأ الإعلان (مفتاح خارجي)
- `deceased_name`: اسم المتوفى
- `funeral_location`: مكان الجنازة
- `funeral_time`: موعد الجنازة (تاريخ ووقت)
- `condolences_location`: مكان العزاء (اختياري)
- `condolences_time`: توقيت العزاء (اختياري)
- `additional_details`: تفاصيل إضافية عن المتوفى
- `created_at`: تاريخ إنشاء الإعلان
- `payment_status`: حالة الدفع (مدفوع، غير مدفوع)
- `is_active`: حالة الإعلان (نشط، غير نشط)

### 3. جدول المدفوعات (payments)
- `id`: معرف فريد للدفع (مفتاح أساسي)
- `announcement_id`: معرف إعلان الوفاة (مفتاح خارجي)
- `user_id`: معرف المستخدم الذي قام بالدفع (مفتاح خارجي)
- `amount`: المبلغ المدفوع
- `payment_method`: طريقة الدفع
- `transaction_id`: معرف المعاملة (من بوابة الدفع)
- `payment_date`: تاريخ الدفع
- `status`: حالة الدفع (ناجح، فاشل، معلق)

## العلاقات بين الجداول

1. **المستخدمين والإعلانات**: علاقة واحد إلى متعدد (مستخدم واحد يمكنه إنشاء عدة إعلانات)
2. **الإعلانات والمدفوعات**: علاقة واحد إلى واحد (كل إعلان له دفعة واحدة)
3. **المستخدمين والمدفوعات**: علاقة واحد إلى متعدد (مستخدم واحد يمكنه إجراء عدة مدفوعات)

## مخطط تدفق البيانات

1. المستخدم يقوم بالتسجيل/تسجيل الدخول
2. المستخدم ينشئ إعلان وفاة جديد
3. المستخدم يدفع رسوم الإعلان
4. النظام يتحقق من حالة الدفع
5. عند نجاح الدفع، يتم تنشيط الإعلان وإرسال إشعارات لجميع المستخدمين
6. المستخدمون يتلقون الإشعارات ويمكنهم عرض تفاصيل الإعلان
